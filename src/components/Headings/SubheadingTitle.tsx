export default function SubheadingTitle({ title }: { title: string }) {
    return (
        <h2 className="text-center font-[900] text-[2.1rem] text-[var(--brown-300)] rounded-sm tracking-[3px] mb-3">{title}</h2>
    )
}
