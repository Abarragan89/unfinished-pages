/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'unfinished-pages.s3.us-east-2.amazonaws.com'],
    },
};

export default nextConfig;
